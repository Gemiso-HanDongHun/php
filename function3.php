<?php

function sum($x, $y){ // 함수의 이름은 sum()이며, 변수 x, y를 매개변수로 가지는 함수를 정의함.

    return $x + $y;  // 매개변수 x, y를 더한 값을 반환함.
    
}

echo sum(1, 2). "\n";      // sum() 함수에 숫자 1와 2을 인수로 전달하여 호출함.

var_dump(sum(1, 2)); // sum() ��������

?>